
function initSummaryBlocks() {

	/*
	var sumLeft = 0;
	var sumRight = 0;
	
	var summaryL = $("div.summaryL");
	var summaryR = $("div.summaryR");
	
	$("div.summaryBlock").each(function(index, value) {

		var block = $(this);
		//block.append("height: ");
		var height = block.height();
		//block.append(height);
		height += 2 + 30; // border + margin

		if (sumLeft <= sumRight) {
		
			block.css("float", "left");
			block.css("clear", "left");
			block.css("margin-left", "0px");
			block.css("margin-right", "0px");
			sumLeft += height; 
			
			block.appendTo(summaryL);
		
		} else {
		
			block.css("float", "right");
			block.css("clear", "right");
			block.css("margin-left", "0px");
			block.css("margin-right", "0px");
			sumRight += height;

			block.appendTo(summaryR);
		}
	});
	*/
}